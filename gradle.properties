group=org.enginehub.lin-bus
version=0.3.0-SNAPSHOT

# Necessary because TeamCity is incompatible with Isolated Projects so we
# disable it generally. But we want to keep Configuration Cache enabled when
# that happens.
org.gradle.configuration-cache=true
org.gradle.isolated-projects=true
